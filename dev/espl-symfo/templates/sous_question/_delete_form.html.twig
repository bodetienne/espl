<form method="post" action="{{ path('sous_question_delete', {'id': sous_question.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_question.id) }}">
    <button class="btn">Delete</button>
</form>
