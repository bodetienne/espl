<form method="post" action="{{ path('piece_jointe_delete', {'id': piece_jointe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece_jointe.id) }}">
    <button class="btn">Delete</button>
</form>
